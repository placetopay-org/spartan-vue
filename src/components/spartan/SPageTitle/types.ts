import type { HTMLAttributes } from 'vue';

export type TPageTitleProps = {
    /**
     * @en Additional CSS classes to customize the appearance of the page title.
     * @es Clases CSS adicionales para personalizar la apariencia del título de página.
     */
    class?: HTMLAttributes['class'];
};
