<script setup lang="ts">
import { ref } from 'vue'
import { usePreview } from '~/composables/usePreview'

const value = ref('')

const { controls } = usePreview({
    props: {
        error: { type: 'boolean', default: true, label: 'error' },
    },
})
</script>

<template>
    <SInputMask v-model="value" mask="00/00/0000" :error="controls.error" placeholder="DD/MM/YYYY" />
</template>
