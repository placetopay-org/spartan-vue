<script setup lang="ts">
import { ref } from 'vue'
import { usePreview } from '~/composables/usePreview'

const value = ref('')

const { controls } = usePreview({
    props: {
        disabled: { type: 'boolean', default: true, label: 'disabled' },
    },
})
</script>

<template>
    <SInputMask v-model="value" mask="00/00/0000" :disabled="controls.disabled" placeholder="DD/MM/YYYY" />
</template>
