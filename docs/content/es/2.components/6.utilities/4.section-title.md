---
title: Section Title
description: Un componente de encabezado semántico para títulos de sección con etiqueta HTML configurable.
navigation:
  icon: i-lucide-heading-1
---

::component-status
::

# Uso

El componente `SSectionTitle` renderiza un elemento de encabezado estilizado. Usa la prop `as` para controlar qué etiqueta HTML de encabezado se utiliza.

::component-preview{file="SSectionTitle/basic"}
::

## Niveles de encabezado

Usa la prop `as` para renderizar diferentes elementos HTML de encabezado (`h1` a `h6`).

::component-preview{file="SSectionTitle/heading-levels"}
::

## Props

| Prop | Tipo | Default | Descripción |
|------|------|---------|-------------|
| `as` | `'h1' \| 'h2' \| 'h3' \| 'h4' \| 'h5' \| 'h6'` | `'h3'` | El elemento HTML de encabezado a renderizar |
| `class` | `string` | - | Clases CSS adicionales |

## Slots

| Nombre | Descripción |
|--------|-------------|
| `default` | Contenido de texto del encabezado |
