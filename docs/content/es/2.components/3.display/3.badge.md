---
title: Badge
description: Un componente compacto en línea para indicadores de estado, etiquetas y categorías con múltiples colores y estilos.
navigation:
  icon: i-lucide-badge
---

::component-status
::

# Uso

El componente `SBadge` muestra una etiqueta compacta en línea. Usa el slot por defecto para establecer el contenido del badge.

::spartan-component
---
items: { color: ['gray', 'white', 'primary', 'red', 'blue', 'green', 'yellow', 'indigo', 'purple', 'neutral'], size: ['sm', 'md', 'lg'] }
props:
  color: gray
  size: md
  dot: false
  outline: false
  pill: false
  removable: false
  reverse: false
slots:
  default: Badge
ignore:
  - class
---
::

## Colores

Usa la prop `color` para cambiar el color del badge.

::spartan-component
---
items: { color: ['gray', 'white', 'primary', 'red', 'blue', 'green', 'yellow', 'indigo', 'purple', 'neutral'] }
props:
  color: primary
slots:
  default: Badge
ignore:
  - size
  - dot
  - outline
  - pill
  - removable
  - reverse
  - class
---
::

## Tamaños

Usa la prop `size` para cambiar el tamaño del badge.

::spartan-component
---
items: { size: ['sm', 'md', 'lg'] }
props:
  color: primary
  size: md
slots:
  default: Badge
ignore:
  - color
  - dot
  - outline
  - pill
  - removable
  - reverse
  - class
---
::

## Outline

Establece `outline` en `true` para mostrar un estilo con borde y fondo más claro.

::spartan-component
---
props:
  outline: true
  color: primary
slots:
  default: Badge
ignore:
  - color
  - size
  - dot
  - pill
  - removable
  - reverse
  - class
---
::

## Pill

Establece `pill` en `true` para usar bordes completamente redondeados.

::spartan-component
---
props:
  pill: true
  color: primary
slots:
  default: Badge
ignore:
  - color
  - size
  - dot
  - outline
  - removable
  - reverse
  - class
---
::

## Dot

Establece `dot` en `true` para mostrar un punto indicador de color antes del contenido.

::spartan-component
---
props:
  dot: true
  color: green
slots:
  default: Activo
ignore:
  - color
  - size
  - outline
  - pill
  - removable
  - reverse
  - class
---
::

## Removable

Establece `removable` en `true` para mostrar un botón de eliminar. El componente emite un evento `removed` al hacer clic. Usa `'stopPropagation'` para también detener la propagación del evento.

::spartan-component
---
props:
  color: primary
  removable: true
slots:
  default: Removable
ignore:
  - color
  - size
  - dot
  - outline
  - pill
  - reverse
  - class
---
::

## Slot Tag

Usa el slot `tag` para mostrar una sección de etiqueta resaltada dentro del badge.

```vue
<template>
  <SBadge color="blue">
    <template #tag>Nuevo</template>
    Funcionalidad lanzada
  </SBadge>
</template>
```

## Reverse

Establece `reverse` en `true` para invertir el orden del tag y el contenido.

```vue
<template>
  <SBadge color="green" reverse>
    <template #tag>v2.0</template>
    Última versión
  </SBadge>
</template>
```

## Combinado

Combina múltiples props para diferentes estilos.

::spartan-component
---
items: { color: ['gray', 'white', 'primary', 'red', 'blue', 'green', 'yellow', 'indigo', 'purple', 'neutral'] }
props:
  color: red
  pill: true
  dot: true
  outline: true
slots:
  default: Crítico
ignore:
  - size
  - removable
  - reverse
  - class
---
::

## Props

| Prop | Tipo | Default | Descripción |
|------|------|---------|-------------|
| `color` | `'gray' \| 'white' \| 'primary' \| 'red' \| 'blue' \| 'green' \| 'yellow' \| 'indigo' \| 'purple' \| 'neutral'` | `'gray'` | Color del badge |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | Tamaño del badge |
| `dot` | `boolean` | `false` | Muestra un punto indicador de color |
| `outline` | `boolean` | `false` | Aplica estilo con borde |
| `pill` | `boolean` | `false` | Aplica bordes completamente redondeados |
| `removable` | `boolean \| 'stopPropagation'` | `false` | Muestra un botón de eliminar. Usa `'stopPropagation'` para detener la propagación del evento |
| `reverse` | `boolean` | `false` | Invierte el orden del tag y el contenido |
| `class` | `string` | - | Clases CSS adicionales |

## Eventos

| Evento | Payload | Descripción |
|--------|---------|-------------|
| `removed` | - | Se emite cuando se hace clic en el botón de eliminar |

## Slots

| Nombre | Descripción |
|--------|-------------|
| `default` | Contenido del badge |
| `tag` | Sección de etiqueta resaltada dentro del badge |
