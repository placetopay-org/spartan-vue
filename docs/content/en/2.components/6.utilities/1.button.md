---
title: Button
description: A versatile button component with multiple styles, variants, and states.
navigation:
  icon: i-lucide-mouse-pointer-click
---

# Usage

The `SButton` component is a versatile button that supports multiple visual variants, sizes, icons, and states.

::spartan-example{title="Basic button"}
<SButton>Click me</SButton>

#code
```vue
<SButton>Click me</SButton>
```
::

## Variants

Use the `variant` prop to change the button's visual style.

::spartan-example{title="Available variants"}
<SButton variant="primary">Primary</SButton>
<SButton variant="secondary">Secondary</SButton>
<SButton variant="danger">Danger</SButton>
<SButton variant="outline">Outline</SButton>
<SButton variant="link">Link</SButton>

#code
```vue
<SButton variant="primary">Primary</SButton>
<SButton variant="secondary">Secondary</SButton>
<SButton variant="danger">Danger</SButton>
<SButton variant="outline">Outline</SButton>
<SButton variant="link">Link</SButton>
```
::

## Sizes

Use the `size` prop to change the button size.

::spartan-example{title="Sizes"}
<SButton size="sm">Small</SButton>
<SButton size="md">Medium</SButton>
<SButton size="lg">Large</SButton>

#code
```vue
<SButton size="sm">Small</SButton>
<SButton size="md">Medium</SButton>
<SButton size="lg">Large</SButton>
```
::

## States

### Disabled

::spartan-example{title="Disabled button"}
<SButton disabled>Disabled</SButton>

#code
```vue
<SButton disabled>Disabled</SButton>
```
::

### Loading

::spartan-example{title="Loading state"}
<SButton loading>Loading...</SButton>

#code
```vue
<SButton loading>Loading...</SButton>
```
::

## Rounded

Use the `rounded` prop to control the border radius.

::spartan-example{title="Rounded borders"}
<SButton rounded="none">None</SButton>
<SButton rounded="left">Left</SButton>
<SButton rounded="right">Right</SButton>
<SButton rounded="both">Both</SButton>
<SButton rounded="full">Full</SButton>

#code
```vue
<SButton rounded="none">None</SButton>
<SButton rounded="left">Left</SButton>
<SButton rounded="right">Right</SButton>
<SButton rounded="both">Both</SButton>
<SButton rounded="full">Full</SButton>
```
::

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `variant` | `'primary' \| 'secondary' \| 'danger' \| 'outline' \| 'link'` | `'primary'` | Visual variant of the button |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | Button size |
| `disabled` | `boolean` | `false` | Disables the button |
| `loading` | `boolean` | `false` | Shows loading state |
| `leftIcon` | `FunctionalComponent` | - | Icon on the left side |
| `rightIcon` | `FunctionalComponent` | - | Icon on the right side |
| `rounded` | `'none' \| 'left' \| 'right' \| 'both' \| 'full'` | `'both'` | Border radius |
| `type` | `'button' \| 'submit'` | `'button'` | HTML button type |
| `as` | `string \| Component` | `'button'` | Element to render |

## Slots

| Name | Description |
|------|-------------|
| `default` | Button content |
