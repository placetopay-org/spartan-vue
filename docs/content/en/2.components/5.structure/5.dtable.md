---
title: DTable
description: Data-driven table component with sorting, loading states, row links, and pagination support.
navigation:
  icon: i-lucide-table
---

::component-status
::

::callout{icon="i-lucide-info"}
**SDTable** is a data-driven table — pass your data array and column definitions, and the table is generated automatically. For a composable table where you control the full HTML structure, see [STable](/en/components/structure/table).
::

# Usage

`SDTable` automatically generates a table from a `data` array and `SDColumn` definitions. Each `SDColumn` maps to a column in the table by matching its `field` prop to a key in the data objects. It supports column sorting, loading skeletons, row links, expandable rows, custom cell rendering, and pagination.

::component-preview{file="SDTable/basic"}
::

## Loading

Set the `loading` prop to `true` to display a skeleton placeholder while data is being fetched. The skeleton automatically matches the number of defined columns.

::component-preview{file="SDTable/loading"}
::

## Sorting

Add the `sort` prop to any `SDColumn` to make that column sortable. Clicking the column header cycles through sort states. The `sort` event is emitted with the column `field` and current `sort` direction.

::component-preview{file="SDTable/sorting"}
::

## Expandable Rows

Add an `SDColumn` with the `expander` prop to enable expandable rows. Each row gets a toggle button that reveals the `expansion` slot content. The `toggleExpanders` event is emitted when a row is expanded, with the row data as payload.

::component-preview{file="SDTable/expandable"}
::

## Row Links

Use the `rowLink` prop to make each row a clickable link. Pass a function that receives the row data and returns a URL. Use `rowLinkAs` to render with a custom component (e.g., `RouterLink`). Add `noLink` to specific columns to exclude them from the link wrapper.

::component-preview{file="SDTable/row-links"}
::

## Pagination

Pass a `paginator` object to display pagination controls below the table. The object follows the `TPaginatorProps` interface and supports `page`, `size`, `total`, `pageSizes`, and more. The `paginatorChange` event is emitted when the user interacts with the controls.

::component-preview{file="SDTable/pagination"}
::

## Slim

Use the `slim` prop to reduce the vertical padding of cells for a more compact layout.

::component-preview{file="SDTable/slim"}
::

## Borderless

Use the `borderless` prop to remove the outer border and shadow of the table.

::component-preview{file="SDTable/borderless"}
::

## SDTable Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `data` | `Record<string, any>[]` | - | Array of row objects to display in the table |
| `loading` | `boolean` | `false` | Shows a skeleton loading state |
| `slim` | `boolean` | `false` | Reduces cell padding for a more compact layout |
| `borderless` | `boolean` | `false` | Removes the border and shadow around the table |
| `rowLink` | `(row: Record<string, any>) => string` | - | Function that returns a URL to make each row a navigable link |
| `rowLinkAs` | `Component` | - | Custom component to render row links (e.g., `RouterLink`) |
| `paginator` | `TPaginatorProps` | - | Paginator configuration object to display pagination controls |
| `class` | `string` | - | Additional CSS classes applied to the table container |

## SDColumn Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `field` | `string` | - | Key in the data object to display in this column |
| `header` | `string` | - | Text displayed in the column header |
| `sort` | `boolean \| 'asc' \| 'des'` | - | Enables sorting on this column. Set to `true` for default, or `'asc'`/`'des'` for an initial direction |
| `noLink` | `boolean` | `false` | Prevents this column from being wrapped in a row link |
| `expander` | `boolean` | `false` | Makes this column an expander toggle for expandable rows |
| `unstyled` | `boolean` | `false` | Removes default cell padding and text styles |

## Events

| Event | Payload | Description |
|-------|---------|-------------|
| `sort` | `{ field: string \| symbol, sort: 'asc' \| 'des' \| boolean }` | Emitted when a sortable column header is clicked |
| `paginatorChange` | `{ page?: number, size?: number, dir?: 'prev' \| 'next' }` | Emitted when the user interacts with pagination controls |
| `toggleExpanders` | `Record<string \| symbol, any>` | Emitted when a row is expanded, with the row data as payload |

## SDColumn Slots

| Name | Props | Description |
|------|-------|-------------|
| `body` | `{ row: any, value: any }` | Custom cell content. `row` is the full data object, `value` is the field value |
| `header` | - | Custom header content for the column |

## SDTable Slots

| Name | Props | Description |
|------|-------|-------------|
| `expansion` | `{ row: any }` | Content displayed when a row is expanded. `row` is the full data object |
