# Docus resolves several of its own transitive dependencies at runtime through
# `importExternalPackage`, which requires a flat node_modules. Without this the
# docs prerender fails to emit llms-full.txt. The library itself does not need
# it: `scripts/checkPhantomDeps.js` guards src/ against undeclared imports.
shamefully-hoist=true
